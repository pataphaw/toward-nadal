# Void v0.2.2 平滑迁移

## 阶段名称
`Void v0.2.2` 契约升级

## Release 强约束对齐（补记）
- 本文档补充结构化 `solution matrix`，用于对齐当前“release 必须对应版本化 solution 集合”的仓库强约束。
- 本次仅补充结构，不改动原有迁移结论与历史描述。

## Solution Matrix（版本化集合）
| 子域 | 版本号 | 版本文档 | 纳入本次 release |
| --- | --- | --- | --- |
| 仓库宪法契约（Void） | `v0.2.2` | [Void](/Users/pataphaw/Projects/toward-nadal/Void) | 是 |
| 仓库协作约束（AGENTS） | 与 `Void v0.2.2` 对齐版 | [AGENTS.md](/Users/pataphaw/Projects/toward-nadal/AGENTS.md) | 是 |
| 04-video-segmentation | 未纳入版本发布 | [视频切片架构（Current）](../solution/04-video-segmentation/current.md) | 否 |
| 05-video-selection | 未纳入版本发布 | [视频精选架构（Current）](../solution/05-video-selection/current.md) | 否 |

说明：
- `2026-04-26-void-v0.2.2-migration` 是一次“契约迁移发布”，不是功能子域（04/05/06/07）版本发布。
- 因此本次 release 的版本化集合聚焦在 `Void` 与 `AGENTS` 契约层。

## 已完成内容
- 将仓库根目录 `Void` 从 `v0.2.0` 升级到 `v0.2.2`
- 将 `AGENTS.md` 的协作约束对齐到新的 `solution -> validation` 迭代规则
- 在 `.void/validation/README.md` 中补充迁移约定，明确旧验证记录保留、增量按新规则执行

## 当前可用范围
- 现有 `.void/solution/` 与 `.void/validation/` 内容可以继续直接使用
- 现有验证文档链接与命名保持不变，不影响已有引用
- 从下一轮方案修改开始，按 `先归档 current -> 再记录 validation -> 再进入下一轮 solution` 的节奏推进

## 已知限制
- 既有 validation 文件尚未统一回填 `solution` 版本、用户反馈与 agent 分析字段
- 既有 validation 文件名未统一切换为版本号命名
- 本次迁移不追溯性重写历史文档，以避免无收益的大规模扰动

## 后续阶段入口
- 当任一子命题进入下一轮正式修改时，先补齐对应 `history/` 与版本化 `validation`
- 若后续发现某类 validation 模板反复使用，再考虑沉淀共享格式示例
