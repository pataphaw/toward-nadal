# 视频切片 V1 边界重推基线验证

## 文档定位
这份记录标记的是一个关键拐点：视频切片 `V1` 从“重放旧边界导出”进入“从源视频重新抽信号并推导边界”的阶段。

它既是 `history/v1` 中的重要阶段证据，也是 `solution/current` 的入口基线证据。

相关文档：
- 目录入口：[Validation](./README.md)
- 子域索引：[视频切片 V1 验证索引](./video-segmentation-v1-validation-index.md)

## 验证对象
- 当前方案：[视频切片架构（Current）](../solution/04-video-segmentation/current.md)
- 历史方案：[视频切片方案历史（V1）](../solution/04-video-segmentation/history/v1.md)
- 证据角色：边界重推基线
- 验证范围：`V1` 从源视频重新抽信号并推导边界的基线能力
- 验证结论：部分通过

## 验证目标
在清理 `.work/clips/img_3026` 历史产物后，不复用旧 `manifest` 的边界结果，而是直接从源视频重新抽信号并推导新的切片边界。

## 本次实现
- 新增脚本：[tools/rederive_video_segmentation.py](/Users/pataphaw/Projects/toward-nadal/tools/rederive_video_segmentation.py:1)
- 输入视频：`.work/raw-videos/IMG_3026.MOV`
- 新运行目录：[20260422-000006](/Users/pataphaw/Projects/toward-nadal/.work/clips/img_3026/20260422-000006)
- 新 `manifest`：[manifest.json](/Users/pataphaw/Projects/toward-nadal/.work/clips/img_3026/20260422-000006/manifest.json)

实现方式：
- 使用 `ffmpeg astats` 按 `0.4s` 窗口提取音频 RMS
- 使用 `ffmpeg` 提取 `2fps` 的低分辨率灰度帧，计算帧间平均绝对差
- 当前边界规则仍是音频优先：
  - 先基于 RMS 构造活跃段
  - 再按短段桥接规则做分组合并
  - 对明显过长的组合，按内部大间隙做拆分
  - 最后加前后缓冲并导出回合主片段

说明：
- 本轮“重新推边界”是成立的，因为边界不是从旧 `manifest` 直接复制。
- 但当前脚本还没有把运动信号真正并入切分决策，运动信息当时主要用于日志和后续调试。

## 信号与计数
- 音频窗口数：`8007`
- 运动窗口数：`6404`
- 运动簇数：`194`
- 音频活跃段数：`88`
- 合并后分组数：`36`
- 最终导出的回合主片段数量：`36`

## 产出结果
- 首段：`point-001`，`105.6s - 117.0s`
- 末段：`point-036`，`3186.4s - 3202.688s`
- 当前仅保留这一次运行目录，没有残留旧切片目录

长片段观察：
- 仍有 `5` 个片段超过 `35s`
- 其中最长的是 `point-009`，`81.8s`
- 这些长片段说明当时的音频优先规则仍会把部分相邻回合粘在一起

## 时间与资源消耗
- 总耗时：`246.523s`
- 峰值 CPU：`918.8%`
- 采样平均 CPU：`603.51%`
- 峰值内存：`1140.44 MB`
- 输出文件总大小：`322089473 bytes`

说明：
- 资源采样是在执行 `ffmpeg` 子进程时通过 `ps` 完成的。
- 这里的 CPU 峰值可理解为大约占满 `9.2` 个逻辑核心。

## 磁盘占用
- `.work`：`4288980 KB`
- `.work/clips`：`355792 KB`
- `.work/raw-videos`：`3933180 KB`
- 本次运行目录：`355780 KB`
- 本次导出片段目录：`330484 KB`

## 当次结论成立的边界
这份记录证明：
- 仓库已经拥有一个可复现的“重推边界”入口。
- 重新推边界不再依赖旧 `manifest`。

这份记录不证明：
- 运动信号已经成为主导切分依据。
- 当前边界质量已经达到“禁止断回合”的偏好。

## 对方案的影响
这份验证带来的真正变化不是参数本身，而是事实来源的改变：
- `rederive` 入口进入仓库，后续验证终于有了统一可追溯入口。
- 方案从“只验证导出重放”前进到“验证完整边界重推”。
- 也正因为这个入口稳定了，后续“运动优先修复”才有明确的落点。

## 结论
- 已完成“从源视频重新推边界”而不是“重放旧边界”。
- 当前脚本可以稳定重跑，且已纳入仓库。
- 现阶段结果可以作为可复现实验基线。
- 但边界质量还不够稳，尤其是若干长片段仍需进一步拆分，因此结论为“部分通过”。
