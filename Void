# Void

- version: v0.2.2
- version-date: 2026-04-26

## 定位

`Void` 是本项目的宪法大纲。
它定义项目生命周期契约、目录语义与使用边界。
具体内容统一存放于 `.void/`。

## 目录契约

### `.void/idea/`

用途：
记录原始想法、动机、需求本质与问题定义。

应写入：
- 为什么要做
- 核心目标
- 要解决的问题
- 用户与场景
- 天然约束与前提

不应写入：
- 详细技术设计
- 具体实施步骤
- 执行中的进度记录

### `.void/blueprint/`

用途：
记录高层蓝图、系统边界与核心关系。

应写入：
- 系统边界
- 核心模块或能力分层
- 关键对象之间的关系
- 主要数据流、控制流或交互流

不应写入：
- 组件级实现细节
- 具体库、框架、接口细节
- 任务拆解与排期

### `.void/solution/`

用途：
记录具体实现方案、决策、取舍与技术细节。
`solution` 是方案唯一事实来源。

应写入：
- 子命题拆分后的方案文档
- 当前生效版本（current）
- 历史版本（history）
- 候选方案及比较
- 关键技术决策与取舍
- 已确认的落地路径

不应写入：
- 日常进度流水账
- 与验证绑定的结果证据
- 阶段性交付总结

命名与版本建议：
- 按子命题拆分目录，目录名使用小写连字符（slug）
- 每个子命题保留一个 `current.md` 作为当前方案
- 历史版本存放在 `history/`，文件名使用版本号（如 `v1.2.0.md`）
- 历史版本仅归档，不参与当前实施

迭代规则：
- 当用户或验证结果触发下一轮 solution 修改前，先将当前 `current.md` 归档为新的历史版本
- 新一轮修改总是基于上一轮 validation 暴露的问题、反馈与原因分析展开
- 不要因为局部微调频繁制造历史版本；只有形成可被验证的一轮方案后才归档

### `.void/validation/`

用途：
记录验证过程、证据、结果、问题与结论。

应写入：
- 关联的 solution 子命题
- 关联的 solution 版本
- 验证人（agent / 人工）
- 用户反馈
- agent 对当前 solution 效果的分析
- 验证结论（通过 / 部分通过 / 不通过）
- 做得好的地方
- 暴露的问题
- 对问题原因的判断
- 已解决项与遗留阻塞

不应写入：
- 纯愿景描述
- 只有计划没有结果的内容
- 与验证无关的实现细节堆积

说明：
- `validation` 使用建议格式，不做强制模板门禁
- 最低要求是可追溯到具体 `solution` 子命题与版本
- 文件名建议直接使用对应的 solution 版本号（如 `v1.2.0.md`）
- 同一版本的 validation 应同时沉淀用户反馈与 agent 的自我分析，为下一轮 solution 提供输入

### `.void/release/`

用途：
记录关键功能完成或关键里程碑达成后的阶段成果。

应写入：
- 阶段名称或版本
- 已实现的关键能力
- 相比上一阶段的主要变化
- 当前可用范围
- 已知限制
- 后续阶段入口

不应写入：
- 详细实施过程
- 原始方案讨论
- 完整验证原始记录

说明：
`release` 不等同于正式对外发布。
凡关键功能实现、关键能力形成、关键里程碑达成，均可构成一次发布。

## 使用原则

1. `Void` 定义规则，`.void/` 承载内容。
2. 新内容优先写入最匹配的目录，避免重复维护。
3. 高层内容放前层，细节内容放后层。
4. `idea -> blueprint -> solution -> validation -> release` 是主要演化路径。
5. `solution`、`validation` 形成持续循环：先归档被验证版本，再记录反馈与分析，再进入下一轮方案修订。
