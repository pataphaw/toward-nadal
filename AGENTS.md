# Repository Guidelines

## 定位
本仓库是基于 `Void` 初始化的具体项目仓库。
`Void` 定义项目宪法与目录契约；`.void/` 承载项目生命周期内容。

## Void 契约
- `Void`：定义规则、边界与目录语义
- `.void/idea/`：记录动机、目标、问题与约束
- `.void/blueprint/`：记录系统边界与高层结构
- `.void/solution/`：记录方案、决策、取舍与版本演进（按子命题拆分）
- `.void/validation/`：记录验证过程、证据、结果与结论（建议格式，非强制）
- `.void/release/`：记录阶段成果、可用范围与已知限制

## 协作规则
- 先判断内容属于代码、`Void` 还是 `.void/`
- 意图、方案、实施、验证、发布相关内容优先进入 `.void/`
- 不在仓库根目录长期堆放零散讨论、进度流水账或验证记录
- 修改 `Void` 时保持克制，只有契约需要变化时才修改

## Agent 协作约束
- 将 `Void` 视为本项目最高层契约
- 发现目录语义与文档位置冲突时，优先维护 `Void` 的一致性
- 基于反馈继续修改 `solution` 前，先归档当前版本，并写入与该版本对应的 `validation`
- 项目特有约束可在本文件追加，但不应删除以上公共约定

## 项目特有约束
在此追加本项目自己的技术、协作或发布要求。

- `validation` 文档必须显式关联到对应的“版本化 solution”（例如 `solution/.../history/vX...md` 或已标注版本号的 `current.md`），这是强约束。
- 新增或更新 `validation` 时，必须在文档中提供可点击的 solution 版本链接；不允许只写笼统“当前方案”而不指向具体版本。
- 若某次变更产出了新的阶段性 solution 版本，需先完成 solution 版本化（含历史快照或明确版本号）再提交对应 validation。
- 一次 `release` 必须对应“一组版本化 solution 的集合”，这是强约束；不允许仅引用笼统 `current` 而不声明具体版本。
- 每个 `release` 文档必须包含结构化 `solution matrix`，至少列出：`子域`、`版本号`、`版本文档链接`、`是否纳入本次 release`。
- 视频切片的基本原则是“完整回合边界”：所有导出视频片段必须以完整回合开始、以完整回合结束；禁止在开头或结尾截断回合（强约束）。
